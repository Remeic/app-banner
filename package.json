{
  "private": false,
  "name": "app-banner",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "lint": "eslint src",
    "microbundle:build": "microbundle",
    "microbundle:watch": "microbundle watch"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^4.5.0",
    "eslint-config-synacor": "^1.1.0",
    "if-env": "^1.0.0",
    "preact-cli": "^2.0.0"
  },
  "dependencies": {
    "goober": "^1.3.1",
    "preact": "^10.0.0-rc.1",
    "preact-render-to-string": "^4.1.0"
  },
  "source": "BannerManager.js",         
  "main": "BannerManager.js"
}
